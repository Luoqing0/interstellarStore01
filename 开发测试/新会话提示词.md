# 星际商店模组 - 会话交接提示词

> 这是上一会话结束时导出的项目状态。将此文件的内容作为新会话的第一个消息发送给 AI，即可无缝接续开发。

---

## 项目基本信息

- **项目路径**: `C:\Steam\steamapps\common\RimWorld\Mods\星际商店文本版\`
- **模组 ID**: `luoqing.starstore`，RimWorld 1.6
- **开发语言**: C#，partial class 模式（12个文件拆分），中文变量名
- **编译**: `cd Source && dotnet build`（需先 cd 到 Source 目录再 build）
- **GitHub**: `https://github.com/Luoqing0/interstellarStore01.git`
- **最新 commit**: `cd670ca` - 回滚干净版本+仅储存区+室内Roofed检测+购买堆叠+dev目录管理
- **规范**: 每次修改完成编译通过后立即 `git add -A && git commit -m "说明" && git push`

## 当前项目状态（截至 2026-06-18）

### 模组功能
- 完整的购买/出售系统，三种预设布局（大/中/小）
- 铁路交易信标投放物品（室外）/ `GenPlace.TryPlaceThing` 直接生成（室内/屋顶下）
- 搜索、分类筛选、模组筛选、仅库存、仅储存区复选框
- 每日折扣、看板娘、购物车面板、数量控制、品质材料选择、出售变体弹窗
- 右键配置交易条件、收藏功能（GameComponent持久化）、设置页面购买/出售价格乘数

### 源码文件（Source目录，共12个）
| 文件 | 职责 |
|---|---|
| `星际商店Mod.cs` | 模组主类，设置界面，数据持久化 |
| `MainTabWindow_星际商店.cs` | 主窗口枢纽，定义所有共享字段和属性 |
| `MainTabWindow_星际商店_标题栏.cs` | 标题、模式切换、搜索框、布局按钮、筛选面板 |
| `MainTabWindow_星际商店_物品网格.cs` | 虚拟滚动物品网格 |
| `MainTabWindow_星际商店_交易.cs` | **最核心**：价格计算、刷新列表、执行购买/出售、帧级缓存、室内生成 |
| `MainTabWindow_星际商店_购物车.cs` | 购物车面板 |
| `MainTabWindow_星际商店_数量控制.cs` | 紧凑5按钮数量控件 |
| `MainTabWindow_星际商店_看板.cs` | 看板面板 |
| `MainTabWindow_星际商店_底部栏.cs` | 底部交易栏 |
| `MainTabWindow_星际商店_配置.cs` | 右键配置窗口 |
| `Window_看板.cs` | 独立看板浮动窗口 |
| `Dialog_SellVariant.cs` | 出售变体选择弹窗 |

### 最近修复的重要 Bug（已完成）
1. **室内地图崩溃**：`!dropSpot.Roofed(map)` 检测 + `获取室内生成点` + `GenPlace.TryPlaceThing` 保底
2. **购买物品堆叠**：while 循环按 `def.stackLimit` 拆分，避免超限物品丢失
3. **"Collection modified"**：index-based while 循环替代 foreach（回滚前已修复）
4. **def.label 空值保护**：部分mod注册了 label=null 的 ThingDef，`刷新物品列表` 中增加 null 检查
5. **仅储存区复选框**：在标题栏仅库存右侧，按 `thing.IsInAnyStorage()` 过滤
6. **仅库存过滤**：按 `thing.Faction` 过滤，仅显示己方派系物品

## 未完成/已知问题
1. 日语文件（`Languages/Japanese/Keyed/星际商店.xml`）缺少 `StarStore_StorageOnly`、`StarStore_PurchaseIndoor`、`StarStore_SaleIndoor` 三个翻译键
2. 室内地图购买大批量物品（如300钢铁）后多次交易可能出现问题（上一轮报错21中未完全验证）
3. 工作区有未暂存的修改 `Defs/SidebarConfigDefs/星际商店侧边栏配置.xml`，需确认是否要提交
4. 工作区有未跟踪的内测文件：`星际商店0.1.0内测版.rar`、`内测说明文档.md`、`反馈模板.txt`、`群公告.txt` 等
5. 英文和日文翻译文件缺失较多翻译键（约缺50-60条），只有简体中文最完整

## 开发测试目录说明
- `开发测试/修改记录/`：所有修改记录文件存放于此（包含本次完整回滚记录）
- `开发测试/报错日志/`：最近两次玩家报错日志（报错20、报错21）
- `开发测试/历史报错/`：更早的报错记录
- `修改记录_*.md` 样式：不要在根目录放，全部移到 `开发测试/修改记录/` 下
- `.gitignore` 已放开 `开发测试/` 的排除，现在该目录受版本控制

## 重要代码片段参考

### 室内/屋顶检测 + 两级投放（`交易.cs` 执行购买）
```csharp
IntVec3 dropSpot = 获取有效降落点(map);
if (dropSpot.IsValid && dropSpot.InBounds(map) && !dropSpot.Roofed(map))
{
    DropPodUtility.DropThingsNear(dropSpot, map, 待生成);
    Messages.Message("StarStore_PurchaseComplete".Translate(), ...);
}
else
{
    IntVec3 center = 获取室内生成点(map);
    for (int i = 0; i < 待生成.Count; i++)
        GenPlace.TryPlaceThing(待生成[i], center, map, ThingPlaceMode.Near);
    Messages.Message("StarStore_PurchaseIndoor".Translate(), ...);
}
```

### 仅储存区过滤（`MainTabWindow_星际商店.cs` 获取库存映射）
```csharp
if (仅储存区 && !thing.IsInAnyStorage()) continue;
```

### 购买堆叠拆分（`交易.cs` 执行购买）
```csharp
int 剩余数量 = kv.Value;
while (剩余数量 > 0)
{
    Thing 物品 = ThingMaker.MakeThing(key.def, stuff);
    // ...品质设置...
    int 本次数量 = Mathf.Min(剩余数量, key.def.stackLimit);
    物品.stackCount = 本次数量;
    待生成.Add(物品);
    剩余数量 -= 本次数量;
}
```

### 获取库存映射中的派系过滤（`MainTabWindow_星际商店.cs`）
```csharp
if (thing.Faction != Faction.OfPlayer && thing.Faction != null) continue;
```

## 建议优先处理
1. 补充日语翻译键
2. 确认 `Defs/SidebarConfigDefs/星际商店侧边栏配置.xml` 的未暂存修改是否需要提交
3. 清理未跟踪的内测文件（决定是否提交到仓库或移出）
4. 完善英文翻译（补齐约50-60条缺失键）