# 修改记录 2026-06-13

## 本轮改动（回滚 + 修复）

### 第一步：回滚到 14:25 版本
- 使用 `git restore` 将全部 8 个修改文件恢复到 commit 9b227fc（回滚前一天的稳定版本）
- 回滚后编译确认 0 错误

### 第二步：建立开发测试文件夹
- 新建 `开发测试/修改记录/` 目录，移动所有修改记录文件
- 新建 `开发测试/报错日志/` 目录，复制报错日志
- 决定：开发测试目录也推送到 GitHub，方便追溯

### 第三步：修复的 Bug

#### Bug 1：仅储存区复选框 + 过滤逻辑
- **标题栏**：将复选框放在"仅库存"右侧（同一行紧凑排列），而非下方
- **主窗口**：新增 `public bool 仅储存区 = false;` 字段
- **获取库存映射**：勾选时跳过 `!thing.IsInAnyStorage()` 的物品
- **语言文件**：新增 `StarStore_StorageOnly`

#### Bug 2：室内地图运输舱崩溃
- **根因**：`DropPodUtility.DropThingsNear` 在屋顶下无法找到有效落点，运输舱刷出界外
- **修复**：`执行购买()` 和 `执行卖出()` 增加 `!dropSpot.Roofed(map)` 检测
- 有屋顶→走 `GenPlace.TryPlaceThing` 在殖民者身边或地图中心直接生成
- 新增 `获取室内生成点(Map map)` 方法
- 新增翻译键 `StarStore_PurchaseIndoor`、`StarStore_SaleIndoor`

#### Bug 3：购买物品堆叠超过 stackLimit
- **修复**：将原来的单次 `stackCount = kv.Value` 改为 while 循环
- 每次取 `Mathf.Min(剩余数量, key.def.stackLimit)`，超过上限时分多个 Thing

#### Bug 4：报错文件中可追溯问题
- 模组筛选功能：已在回滚版本中正确实现（`刷新物品列表` 初始化 `可选模组列表`）
- Collection modified：已在回滚版本中用 index-based while 循环解决
- 仅库存所有权过滤：已在回滚版本中通过派系过滤正确实现