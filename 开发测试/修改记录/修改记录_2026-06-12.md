# 修改记录 2026-06-12

## 修改概述

修复 5 个问题 + 看板视觉独立

## 详细修改

### 1. 侧边栏位置修复
- **根因**：DLL 未编译，源码改动未生效
- **文件**：`Source/MainTabWindow_星际商店.cs`
- **内容**：每页物品数从 `const int 50` 改为 `int 每页物品数 => 行数 * 列数`，侧边栏改为最后绘制（z-order 正确）

### 2. 问候抖动修复
- **根因**：`Rand.Range` 每帧调用
- **文件**：`Source/MainTabWindow_星际商店.cs`
- **内容**：`PreOpen()` 中缓存问候文字到 `缓存问候文字` 字段

### 3. 布局修复
- **根因**：`每页物品数 = 50` 导致所有物品挤在一页
- **内容**：改为 `行数 * 列数`（大布局 = 8），分页正常

### 4. 分类按钮迁移
- **文件**：`Source/MainTabWindow_星际商店_标题栏.cs`
- **内容**：水平分类标签页改为垂直分类列，宽 80px，放在看板与主网格之间

### 5. 数量控制消失修复
- **根因**：`控Y` 依赖动态 `名称Rect` 高度，品质/材料/折扣撑高后超出格子底部
- **文件**：`Source/MainTabWindow_星际商店_物品网格.cs`
- **内容**：数量控制固定在格子底部 `rect.yMax - 内边距 - 24f`

### 6. 看板独立面板
- **文件**：`Source/MainTabWindow_星际商店_侧边栏.cs` → `Source/MainTabWindow_星际商店_看板.cs`
- **内容**：
  - 全部变量/方法从"侧边栏"重命名为"看板"
  - 配色改为深色报纸风（深棕底色 + 暗金标题 + 暖灰文字）
  - 看板右侧加垂直分隔线，强化独立面板视觉
  - 与商店蓝紫科技风形成明显对比

### 7. 编译修复
- 修复 `分类区域` 未定义编译错误（筛选面板/配置窗口引用改为 `标题区域`）

## 编译结果
- 0 错误 0 警告