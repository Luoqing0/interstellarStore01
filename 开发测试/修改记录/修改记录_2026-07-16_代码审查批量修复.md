# 修改记录 — 2026-07-16 代码审查批量修复

## 基本信息
- **日期**: 2026-07-16
- **涉及文件**: 6 个 C# 源文件
- **对应反馈**: Steam 玩家 猫与星象仪 报告"动物没法卖+输入框光标乱飘"

## 修改主题

基于全面代码审查报告（4高+6中+12低），批量修复所有高严重度和中严重度问题，以及部分低严重度问题。

## 修复清单

### 高严重度（4/4 全部修复）

| 编号 | 问题 | 修复方案 |
|------|------|---------|
| H1 | 仅显示库存过滤遗漏机械族 | mapPawns 遍历增加 `p.RaceProps.IsMechanoid` 检查 |
| H2 | TransactionKey 严格匹配导致变体物品卖出失败 | 新增 `宽松匹配` 方法，任一方 quality/stuff 为 null 时视为通配符 |
| H3 | Pawn 有 CompQuality 时变体弹窗为空 | `物品有变体` 方法增加 `def.race != null` 排除 Pawn |
| H4 | Dialog_SellVariant 输入框无持久化缓冲 | VariantRow 增加 `数量缓冲` 字段，使用持久化缓冲替代每帧重建 |

### 中严重度（6/6 全部修复）

| 编号 | 问题 | 修复方案 |
|------|------|---------|
| M1 | 卖出 SplitOff 返回值未销毁 | 检查返回值并 Destroy |
| M2 | 数量输入缓冲模式切换/清空时未清理 | 标题栏模式切换 + 底部栏清空 + PreClose + 交易完成 4 处添加 Clear |
| M3 | PreOpen 中 Find.CurrentMap 可能 NRE | 添加 null 检查 |
| M4 | 动物未在"全部"卖出分类中显式添加 | 类似机械族通过 Union 添加殖民地动物种族 |
| M5 | 缓冲 sync 检查 StartsWith("TextField") 过宽 | 改用精确控件名匹配 `TextField+y+x` |
| M6 | 机械族/动物预检查与执行之间重复查询 AllPawns | 阶段1收集 Pawn 引用存入 candidates，阶段2直接使用 |

### 低严重度（2/12 修复）

| 编号 | 问题 | 修复方案 |
|------|------|---------|
| L6 | 数量输入缓冲字典无限增长 | PreClose 中 Clear |
| L7 | 交易总额为 0 时静默返回 | 添加 "购物车为空" 提示消息 |

### 未修复的低严重度（10 项，不影响功能/性能影响极小）

- L1: 每帧分配新 List（GC 压力极小）
- L2: 购物车价格重复计算（有帧级缓存）
- L3: 分类查询每次创建新 HashSet（可优化但不影响功能）
- L4: 每帧创建 Color 对象（数量小）
- L5: PawnKindDef 线性搜索（仅在购买时）
- L8: 设置中默认行数/列数未使用（设计决策，非 bug）
- L9: 变体弹窗单价用第一件 HP（显示问题，不影响交易）
- L10: 缓存格子尺寸分页切换（极低概率视觉问题）
- L11: 跳转页码输入未清理（影响极小）
- L12: 机械族/动物出售价格不含品质因素（设计决策，Pawn 用 MarketValue 已反映价值）

## 编译结果
- 0 警告，0 错误

## 涉及文件
1. `Source/星际商店Mod.cs` — TransactionKey.宽松匹配
2. `Source/MainTabWindow_星际商店.cs` — 物品有变体排除 Pawn + PreClose 清缓冲 + PreOpen null 检查
3. `Source/MainTabWindow_星际商店_交易.cs` — 宽松匹配 + SplitOff 销毁 + 仅显示库存加机械族 + Union 加动物 + M6 优化 + L7 提示
4. `Source/MainTabWindow_星际商店_数量控制.cs` — 宽松匹配 + M5 精确控件名
5. `Source/MainTabWindow_星际商店_标题栏.cs` — M2 模式切换清缓冲
6. `Source/MainTabWindow_星际商店_底部栏.cs` — M2 清空购物车清缓冲
7. `Source/Dialog_SellVariant.cs` — H4 持久化缓冲

## 测试要点
1. 卖出有品质/材料变体的物品（如武器、服装）—— >> 按钮应正确显示库存数量，点击卖出应成功
2. 卖出动物和机械族—— >> 按钮和卖出执行都应正常工作
3. 卖出模式"仅显示库存"—— 动物和机械族都应出现在列表中
4. 输入框输入数量—— 光标不应跳变（主窗口和变体弹窗）
5. 搜索框输入时—— +/- 按钮修改的数量应同步到输入框
6. 切换购买/卖出模式—— 输入框应清空
7. 购物车为空时点交易—— 应提示"购物车为空"
